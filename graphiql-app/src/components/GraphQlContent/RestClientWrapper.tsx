import dynamic from "next/dynamic";
//const RestClient = dynamic(() => import("./RestClient"), { ssr: false });

export default function RestClientWrapper() {
  return (
    <div>
  {/*
      <RestClient />
  */}
    </div>
  );
}
